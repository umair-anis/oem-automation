'use strict'

let { buildElement } = require('../../../../core/element/buildElement')
let { login } = require('./loginUi')

/**
 * The UI elements present on the Admin Portal Login page
 * @returns UI Object
 */
let adminLoginUI = async () => {

    // TODO: inherit login funcitonality for admin but with a 
    // different browser link - http://admin-portal-qa.connectedfleet.io/#/login


    return this
}

module.exports = {
    adminLoginUI
}