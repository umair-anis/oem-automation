'use strict'

/**
 * @description
 * @function createScenarioResult
 * @param {*} scenario
 * @param {*} reporterConfig
 * @async
 * @returns {ScenarioResult}
 * @example
 */
const createScenarioResult = async (scenario = {}, reporterConfig = {}) => {

  // get test result
  // test result id
  // current scenario number
  // scenario name
  // start time
  // total step count
  // browser name
  // current data iteration

  // returns an instance of the scenario result
}

module.exports = {
  createScenarioResult
}
