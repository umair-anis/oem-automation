'use strict'

/**
 * @description
 * @function createStepResult
 * @param {*} step
 * @param {*} reporterConfig
 * @async
 * @returns {StepResult}
 * @example
 */
const createStepResult = async (step = {}, reporterConfig = {}) => {
  // scenario id
  // current step number
  // action name
  // step start time
  // if has child then element name and hasChild = true
  // return the stepResult
}

module.exports = {
  createStepResult
}
