'use strict'

const writeTraceLog = async (test = {}) => {
  // Write qTest TC ids to a Google spreadsheet used for reporting so management can view the relationship between automated and manual tests.
}

module.exports = {
  writeTraceLog
}
