'use strict'

const { getJunitXml } = require('junit-xml')

/**
 * @description
 * @function log
 * @param {*} result
 * @async
 * @example
 */
const log = async (result = {}) => {

}

module.exports = {
  log
}
