'use strict'

const { cloneDeep } = require('lodash')

const ConnectionInfoBuilder = (() => {
  const connectionInfo = {
    //connection string?
    //database name?
    //table name?
  }

  this.build = () => {

  }

  return this
})

module.exports = {
  ConnectionInfoBuilder
}
